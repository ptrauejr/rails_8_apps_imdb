%h2= @play.title
%h3= @play.director
%h4= @play.category.name
%p= @play.description

.btn-group
  - if user_signed_in?
    -if @play.user_id == current_user.id
      = link_to "Edit", edit_play_path(@play)
      = link_to "Delete", play_path(@play), method: :delete, data: { confirm: "Are you sure?" }